// ─── WealthOS Sync ─────────────────────────────────────────────────────────────
// Superseded — all pages now read/write directly to the API.
// Stub kept so existing <script src="sync.js"> tags load without errors.
window.WOS_SYNC = { run: function () {} };
